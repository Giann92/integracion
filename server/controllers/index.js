const getChar = require('../controllers/getCharById');
const login = require('../controllers/login');
const handleFavotites = require("../controllers/handleFavorites");

module.exports = {
    getChar,
    login,
    handleFavotites,
}